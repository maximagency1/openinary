[build]
builder = "DOCKERFILE"
dockerfilePath = "/docker/full.Dockerfile"
dockerLayerCaching = true

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "3000"
